Python script for extracting elastic moduli from VASP OUTCAR and calculating some useful properties.

Note total elastic tensor is used. 

Note test VASP for convergence of these properties with cutoff energy, they can require a higher than normal cutoff.
